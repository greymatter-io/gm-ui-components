import styled from "styled-components";

import { spacingScale } from "style/styleFunctions";

const SectionIcon = styled.span`
  flex: 0 0 auto;
  padding-right: ${spacingScale(0.5)} !important;
`;

export default SectionIcon;
